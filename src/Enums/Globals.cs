namespace ReversiGame
{
    public static class Globals
    {
        public enum Signal
        {
            ON_CLICK,
            ON_BLACK_WIN,
            ON_WHITE_WIN,
            ON_DRAW,
        }
    }
}